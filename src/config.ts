/**
 * Shared configuration for the Electron app
 * Note: Change DOWNSCALE_FACTOR in config.json to change it for the capture loop
 */

// Downscale factor - controls how much the captured image was reduced
// Must match config.json DOWNSCALE_FACTOR
export const DOWNSCALE_FACTOR = 1;